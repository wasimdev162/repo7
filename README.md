# HFT Execution Algorithm (Simulation)

This repository contains a Rust-based HFT-style execution algorithm scaffold with
microstructure signals, execution tactics, and performance reporting. The
implementation lives in `hft_execution/`.

See `hft_execution/README.md` for architecture, configuration, and run steps.